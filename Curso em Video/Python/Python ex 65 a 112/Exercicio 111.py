# criar um pacote com dois modulos, moeda e dado, 
# passar as funções dos desafios 107,108,109,110, e manter funcionando

from utilidadescev import moeda
p = float(input('Digite um valor : '))
moeda.resumo(p, 80 , 35) # (valor, aumento, redução)
