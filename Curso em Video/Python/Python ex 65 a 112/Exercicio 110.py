from modulos import moeda
# Adicione a moeda.py yma função resumo();
# que mostra o resumo do que ja foi criado ate aqui.

p = float(input('Digite um valor : '))
moeda.resumo(p, 80 , 35) # (valor, aumento, redução)
